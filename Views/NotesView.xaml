<Page x:Class="DDGameMaster.Views.NotesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      mc:Ignorable="d" 
      Title="NotesView" Height="600" Width="800">
    <Grid Background="#2D2D30">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="DM Notes" Foreground="White" FontSize="32" HorizontalAlignment="Center" Margin="0,10,0,10"/>
            
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Save Notes" Width="150" Height="40" Margin="10" Click="SaveNotes_Click"/>
                <Button Content="Load Notes" Width="150" Height="40" Margin="10" Click="LoadNotes_Click"/>
                <Button Content="Back" Width="150" Height="40" Margin="10" Click="Back_Click"/>
            </StackPanel>
            
            <TextBox 
                x:Name="NotesTextBox"
                AcceptsReturn="True" 
                TextWrapping="Wrap" 
                VerticalScrollBarVisibility="Auto"
                Background="#3E3E42"
                Foreground="White"
                FontSize="14"
                BorderThickness="1"
                BorderBrush="Gray"
                Margin="10"/>
        </DockPanel>
    </Grid>
</Page>