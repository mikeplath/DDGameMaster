<Page x:Class="DDGameMaster.Views.CharacterSheetView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      mc:Ignorable="d" 
      Title="CharacterSheetView" Height="600" Width="800">
    <Grid Background="#2D2D30">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Character Sheet" Foreground="White" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="15" MinWidth="450">
                <StackPanel>
                    <!-- Basic Info -->
                    <TextBlock x:Name="NameTextBlock" Foreground="White" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="RaceClassLevelTextBlock" Foreground="LightGray" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    
                    <!-- HP Display -->
                    <TextBlock x:Name="HitPointsTextBlock" Foreground="IndianRed" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                    
                    <!-- NEW HP MANAGEMENT CONTROLS -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,15">
                        <Button Content="Damage" Width="80" Height="30" Margin="5" Click="Damage_Click"/>
                        <TextBox x:Name="HpAmountTextBox" Width="60" Height="30" Text="1" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                        <Button Content="Heal" Width="80" Height="30" Margin="5" Click="Heal_Click"/>
                    </StackPanel>
                    
                    <!-- Stats Grid -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" x:Name="StrengthTextBlock"      Foreground="White" FontSize="16" Margin="10"/>
                        <TextBlock Grid.Row="1" Grid.Column="0" x:Name="DexterityTextBlock"     Foreground="White" FontSize="16" Margin="10"/>
                        <TextBlock Grid.Row="2" Grid.Column="0" x:Name="ConstitutionTextBlock"  Foreground="White" FontSize="16" Margin="10"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="IntelligenceTextBlock"  Foreground="White" FontSize="16" Margin="10"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="WisdomTextBlock"        Foreground="White" FontSize="16" Margin="10"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="CharismaTextBlock"      Foreground="White" FontSize="16" Margin="10"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Button Content="Back" Width="200" Height="40" Margin="0,30,0,0" Click="Back_Click"/>
        </StackPanel>
    </Grid>
</Page>